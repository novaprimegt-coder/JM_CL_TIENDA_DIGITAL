name: Diagnostico GitHub Actions JM Cruz L. Digital

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  diagnostico:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar runner
        run: |
          echo "GitHub Actions runner operativo"
          uname -a
