name: Diagnostico JM Cruz L. Digital

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar GitHub Actions
        run: echo "GitHub Actions operativo"
